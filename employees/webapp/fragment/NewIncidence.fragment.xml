<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Panel>
        <headerToolbar>
            <Toolbar>
                <core:Icon
                    src="{=
                ${form>Type} === '0'? 'accept':
                ${form>Type} === '1'? 'alert' :
                ${form>Type} === '2'? 'error' : ''
                }"
                />
                <Title
                    text="{i18n>incidence} ({form>Index})"
                    wrapping="true"
                />
                <ToolbarSpacer/>
                <Button 
                        icon="sap-icon://save"
                        press="onSavePress"
                        type="Emphasized"
                        enabled="{= ${form>EnabledSave} === true}" />
                <Button 
                        icon="sap-icon://delete"
                        press="onDeletePress"
                        type="Reject" />
            </Toolbar>
        </headerToolbar>

        <content>
            <HBox>
                <VBox class="sapUiSmallMarginEnd">
                    <Label text="{i18n>date}:" required="true" />
                    <DatePicker dateValue="{form>CreationDate}"
                                change="updateIncidenceDate"
                                valueState="{form>DateState}" />
                </VBox>
                <VBox class="sapUiSmallMarginEnd">
                    <Label text="{i18n>reason}:" required="true"/>
                    <Input value="{form>Reason}"
                            change="updateIncidenceReason"
                            valueState="{form>ReasonState}" />
                </VBox>
                <VBox class="sapUiSmallMarginEnd">
                    <Text text="{i18n>type}:" />
                    <Select selectedKey="{form>Type}"
                            change="updateIncidenceType">
                        <core:Item
                            key="-1"
                            text="{i18n>empty}"
                        />
                        <core:Item
                            key="0"
                            text="{i18n>incidenceCorrect}"
                        />
                        <core:Item
                            key="1"
                            text="{i18n>incidencePending}"
                        />
                        <core:Item
                            key="2"
                            text="{i18n>incidenceError}"
                        />
                    </Select>
                </VBox>
            </HBox>
        </content>
    </Panel>
</core:FragmentDefinition>
