<mvc:View
    controllerName="com.logali.employees.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:f="sap.f"
    xmlns="sap.m"
    xmlns:filterbar="sap.ui.comp.filterbar"
    xmlns:core="sap.ui.core"
>
    <f:DynamicPage>
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title
                        text="{i18n>appTitle}"
                        wrapping="true"
                    />
                </f:heading>
            </f:DynamicPageTitle>
        </f:title>

        <f:header>
            <f:DynamicPageHeader>
                <filterbar:FilterBar
                    search="onFilterSearch"
                    showClearOnFB="true"
                    clear="onClearPress"
                >
                    <filterbar:filterGroupItems>
                        <filterbar:FilterGroupItem
                            groupName="filters"
                            name="employee"
                            label="{i18n>labelEmployee}"
                            visibleInFilterBar="true"
                        >
                            <filterbar:control>
                                <Input id="filterEmployee" />
                            </filterbar:control>
                        </filterbar:FilterGroupItem>

                        <filterbar:FilterGroupItem
                            groupName="filters"
                            name="country"
                            label="{i18n>labelCountry}"
                            visibleInFilterBar="true"
                        >
                            <filterbar:control>
                                <MultiComboBox
                                    id="filterCountry"
                                    selectionFinish="onSelectionFinish"
                                    items="{
                                        path:'countries>/Countries',
                                        templateShareable: true
                                        }"
                                    showSecondaryValues="true"
                                >
                                    <items>
                                        <core:ListItem
                                            key="{countries>code}"
                                            text="{countries>country}"
                                            additionalText="({countries>code})"
                                        />
                                    </items>
                                </MultiComboBox>
                            </filterbar:control>
                        </filterbar:FilterGroupItem>
                    </filterbar:filterGroupItems>
                </filterbar:FilterBar>
            </f:DynamicPageHeader>
        </f:header>

        <f:content>
            <Table
                id="table"
                items="{employees>/Employees}"
                growing="true"
                growingScrollToLoad="true"
                autoPopinMode="true"
            >
                <headerToolbar>
                    <Toolbar>
                        <Title
                            core:require="{
                                Formatter: 'com/logali/employees/model/formatter'
                            }"
                            text="{
                                path: 'view>title',
                                formatter: 'Formatter.titleFormatter.bind($controller)'
                            }"
                            wrapping="true"
                        />
                    </Toolbar>
                </headerToolbar>

                <columns>
                    <Column
                        demandPopin="true"
                        minScreenWidth="Phone"
                        importance="High"
                    >
                        <Text text="{i18n>ID}" />
                    </Column>
                    <Column
                        demandPopin="true"
                        minScreenWidth="Phone"
                        importance="High"
                    >
                        <Text text="{i18n>fullName}" />
                    </Column>
                    <Column
                        demandPopin="true"
                        minScreenWidth="Tablet"
                        importance="Medium"
                    >
                        <Text text="{i18n>country}" />
                    </Column>
                    <Column
                        demandPopin="true"
                        minScreenWidth="Tablet"
                        importance="Medium"
                    >
                        <Text text="{i18n>city}" />
                    </Column>
                    <Column
                        demandPopin="true"
                        minScreenWidth="Desktop"
                        importance="Low"
                    >
                        <Text text="{i18n>postalCode}" />
                    </Column>
                </columns>

                <items>
                    <ColumnListItem type="Navigation"
                                    press="onNavToDetails">
                        <cells>
                            <ObjectIdentifier title="{employees>EmployeeID}" />
                            <Text
                                text="{employees>LastName} {employees>FirstName}"
                            />
                            <Text text="{employees>Country}" />
                            <Text text="{employees>City}" />
                            <Text text="{employees>PostalCode}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </f:content>
    </f:DynamicPage>
</mvc:View>
